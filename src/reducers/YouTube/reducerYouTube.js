import * as actionTypes from './actionTypes';

let initialState = {

};

const youTube = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    };
};

export default youTube;